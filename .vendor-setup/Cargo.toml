[package]
name = "windows-reactor-setup"
version = "0.100.0"
edition = "2024"
rust-version = "1.95"
license = "MIT OR Apache-2.0"
description = "Windows App SDK runtime setup for windows-reactor"
repository = "https://github.com/microsoft/windows-rs"
categories = ["os::windows-apis"]
readme = "readme.md"

# Vendored from microsoft/windows-rs tag 74 (WASDK 2.4.0).
# Kept as a path crate so Cargo does not unify this checkout with the
# hooks-era windows-core git rev used by `.vendor`.
